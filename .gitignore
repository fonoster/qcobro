node_modules
dist
**/generated
*.db
*.db-journal
.env
.env.local
*.tsbuildinfo
coverage
storybook-static
.DS_Store

# Local secrets for the Identity service (RS256 keys, etc.)
mods/identity-service/keys/

# Local service config (holds dev secrets). Commit qcobro-prod.json as the example.
qcobro.json
