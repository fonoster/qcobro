node_modules
dist
**/generated
*.db
*.db-journal
.env
.env.local
*.tsbuildinfo
coverage
storybook-static

# Generated SDK API reference (run `npm run docs` to regenerate)
mods/sdk/docs
.DS_Store

# Local service config (holds dev secrets). Commit qcobro.example.json as the example.
qcobro.json
!qcobro.example.json

# Local Identity service config + keys (holds dev secrets).
# Commit config/identity/identity.example.json as the example.
config/identity/identity.json
config/identity/keys/

# Playwright
node_modules/.cache
test-results/
playwright-report/
playwright/.cache/

# Pencil design backups (active file is pencil.pen)
*-old.pen
