services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: qcobro
      POSTGRES_PASSWORD: qcobro
      POSTGRES_DB: qcobro
    ports:
      - "5432:5432"
    volumes:
      - qcobro-db:/var/lib/postgresql/data

volumes:
  qcobro-db:
