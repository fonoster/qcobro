{
  "name": "@qcobro/identity-service",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Slim packaging of Fonoster Identity as a standalone gRPC service",
  "scripts": {
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts",
    "db:push": "prisma db push --schema prisma/schema.prisma --skip-generate"
  },
  "dependencies": {
    "@fonoster/common": "^0.18.2",
    "@fonoster/identity": "^0.18.2",
    "@grpc/grpc-js": "^1.10.6"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.9.0"
  }
}
