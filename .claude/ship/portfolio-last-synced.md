# Ship checkpoint — portfolio-last-synced

Started: 2026-07-12
Current stage: 6 — Archive (done)

**Scope:** Add a `lastSyncedAt` timestamp to `Portfolio`, set on CSV sync completion, exposed via `portfolios.list`/`portfolios.read`, and shown as a "last synced" column in the portfolio list table (with a "Never synced" placeholder). Working autonomously overnight per user request; will open a PR for review rather than merging.

**Detected surfaces:** OpenSpec: yes · Pencil: yes (`pencil.pen`) · Storybook: yes (`mods/webapp/.storybook`) · E2E: yes (`playwright.config.ts`, `e2e/`)

| #   | Stage           | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| :-- | :-------------- | :----- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Frame           | done   | No existing change matched; created `portfolio-last-synced` via `/opsx:propose`.                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 1   | Design (Pencil) | done   | Reconciled portfolio list table (`umfHB` in `pencil.pen`, "Carteras · Lista" screen) against shipped code columns instead of redesigning: the "Sincronizado" date column already existed in the design (last-synced was already modeled) but `clientId`, `totalOutstandingBalance`, `recoveredAmount` were missing even though shipped in code. Renamed Name sub-line from placeholder "Segmento: X" to "Cliente: X", added Saldo/Recuperado columns. Select/Status columns remain disabled (unused) as before. |
| 2   | Spec reconcile  | done   | No behavior changed since design; delta specs (`portfolios`, `portfolio-accounts`, `web-console`) already cover `lastSyncedAt`. `openspec validate portfolio-last-synced` passes.                                                                                                                                                                                                                                                                                                                               |
| 3   | Build           | done   | Schema + migration (`20260713031839_portfolio_last_synced`), `@qcobro/common` types, `syncAccounts.ts` stamping, webapp column + i18n (en/es).                                                                                                                                                                                                                                                                                                                                                                  |
| 4   | Test            | done   | Unit: `syncAccounts.test.ts` (stamping + validation-failure no-write case). E2E: new `e2e/portfolio-last-synced.spec.ts`; full suite (20 specs) + apiserver/common/sdk unit suites (226 tests) all green. Lint + typecheck clean across all 4 workspaces.                                                                                                                                                                                                                                                       |
| 5   | Sync            | done   | Promoted delta specs into `portfolios`, `portfolio-accounts`, `web-console` main specs.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 6   | Archive         | done   | Moved to `openspec/changes/archive/2026-07-13-portfolio-last-synced/`.                                                                                                                                                                                                                                                                                                                                                                                                                                          |

Status values: `pending` · `in-progress` · `done` · `skipped` (with reason).

## Decision log

Newest first. One line per meaningful decision or stage transition.

- 2026-07-12 — User going to bed; authorized autonomous decisions through sync/archive, ending in a PR (not a merge) for morning review, plus a `/code-review low` pass before opening it.
- 2026-07-12 — Design stage: user clarified Pencil already partially covers this (the "Sincronizado" date column) but is missing fields already shipped in code (clientId, balance, recovered) — task was to add those, not redesign the last-synced column.
- 2026-07-12 — Stashed an unrelated pre-existing `compose.yaml` local edit (port mapping) before branching, to keep it out of this change; left stashed on `main` for the user to reapply.
- 2026-07-12 — Working on branch `feat/portfolio-last-synced`.
- 2026-07-12 — Change created via `/opsx:propose`; proposal/design/tasks/specs all written and validated.
